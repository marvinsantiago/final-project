<div class='events'>
    <h4>{{#link-to 'events'}}Upcoming Events{{/link-to}}</h4>
    <ul>
        {{#each model.events as |event|}}
            <li>{{#link-to 'events'}}{{fa-icon 'fa-angle-double-right'}} {{event.title}}{{/link-to}}</li>
        {{/each}}
    </ul>
</div>

{{outlet}}

